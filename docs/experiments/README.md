# `docs/experiments/` — index

One directory per study: a `REPORT.md` written on top of numbers generated
deterministically from the run's own CSVs, plus the generated tables and figures.
These are the **record of what a run produced** — they are archives, not plans,
and they are not pruned when the work they justified ships.

**Every study directory is named `YYYY-MM-DD-<slug>`**, dated when its report
first landed, so a bare `ls` sorts the archive chronologically and the newest
work is the last thing printed. The date is the *study's*, not its last edit: a
report that is later corrected, re-skinned or extended keeps the name it was
filed under, because that name is what every link in the tree points at. The
index below runs the other way — **newest first**. `scripts/check-docs.py`
enforces both halves: the name shape, and that every study has a row here.

**What a report owes its reader** (the long form is in the `grid-experiments`
skill): two significant digits unless a decision needs a third, arm differences
**paired** and carrying a standard error; **figures** emitted by a committed
script from the same CSVs as the tables; and **literal examples** of the errors
behind every error rate, so a reader can tell a model error from an annotation
error. Analysis scripts must be committed: the doc gate checks that paths cited
here resolve.

**Every study that simulates a user clicking owes the quality-over-clicks pair**
— how good that user's detector is as they click more, **averaged** (one panel
per dataset, one line per arm) and again **per run** (one panel per arm, every
seed as its own line). Click 0 is the free text sort, so the far left is what
typing was worth and the far right is what clicking was worth. One
implementation, [`scripts/experiments/calibration/curves.py`](../../scripts/experiments/calibration/curves.py);
do not write it again.

**…and it owes an interactive `viewer.html`, linked from the report.** The PNGs
answer the questions the analyzer asked; the viewer is what lets a reader ask
their own — any dataset (one / all / each), any category (one / all / each), any
subset of arms, any subset of embedders, seeds averaged or every seed, and any
metric the run emitted (cost, precision, recall, F1, FPR, FNR, average
precision, AUROC). Two draw toggles decide the shape: **overlay** puts every
varying dimension on one chart in distinct hues (off, each gets its own chart
with the ±1 SD shadow, which is the only place that shadow is readable), and
**oracle threshold** adds the cut the test labels say the model should have
used, dotted beside the solid line it achieved. Two more reference marks are
notched into the margins: the free text sort at the left and, for a run
launched with `CALIB_SKYLINE_ARMS`, the supervised skyline at the right. Built
by [`scripts/experiments/calibration/viewer.py`](../../scripts/experiments/calibration/viewer.py)
from the same CSVs as everything else, self-contained, no network.

**A page can be re-skinned without its results.** The template carries all of
the viewer's behaviour and the payload only the study's numbers, so
`python viewer.py --reskin docs/experiments/*/viewer.html` pushes a template
improvement onto every committed report — even one whose results directory is
long gone. It cannot add data the payload never carried, so a *series* still
needs a rebuild from the cell CSVs. Both of the ones added in #3325 were
backfilled that way in #3326: the oracle cut cost nothing to recover (its inputs
are on every base row ever emitted), and the skyline was measured by a second,
cheaper pass over the same cells and merged in with `--skyline-results`, because
it is vote-independent and re-running the loop for it would have replaced the
performance rows the reports' tables were read off.

**A long study also gets a `report.html` reading copy, in the same directory.**
`REPORT.md` is the archive and renders inline on GitHub, but bitmap plots cannot
be zoomed and a 900-line document wants a table of contents. `report.html` is a
self-contained page — vector figures inlined, photographs embedded, one file,
opened in a browser (GitHub renders raw HTML as source). **Generate it, never
hand-write it:** `scripts/experiments/calibration/make_bench_html.py` builds it
from the study's own `REPORT.md`, so the narrative has one source and the two
cannot drift — edit the report, re-run the script, commit both. It lives beside
the markdown it was built from precisely so a reader never has to choose which
of two directories a study is in.

**A few early studies are narrative-only**, from before the CSV-and-figures
convention: their `report.html` *is* the record and the `REPORT.md` beside it is
a short pointer carrying the question and the verdict. Their rows below say so.

**The other archive.** [`docs/plans/`](../plans/) holds work still owed.

Add a row here whenever you add a study — at the top, since the table runs
newest first.

| Study | Question | Verdict |
|-------|----------|---------|
| [`2026-09-06-provable-negatives-3670/`](2026-09-06-provable-negatives-3670/REPORT.md) | Take `vg_scale`'s shared negative pool from 3,900 to 9,900 so a band cell sits at 1% prevalence instead of 2.5% (#3670), and decide what the extra negatives are made of: every one COCO-scored (`provable`), or matched to the positives' own 57% COCO share (`matched`). The pool is redrawn off the loader's own passes rather than rebuilt, so every number here is the number a build produces. | **The pool is `provable`, but not for the reason the trade was first written down with — and the headline number is 0.85%, not 1%.** A cell has scored the other eleven classes' COCO-scored positives since #3667, so the realised pool is ~1,900 larger than `SCALE_N_NEG`: designed **1.00%**, realised **0.85%** (0.844-0.856 over 36 cells, k\* **-6.87**). On composition, both candidates distort and on #3667's FPR scale they are **not separable** — the provenance shortcut an all-provable pool creates is **1.1x** (reverse arm, 1.06-1.12 over three embedders) against contamination's **1.18x** [1.09, 1.37] at #3666's *measured* 1.40%. An earlier reading quoted 1.32x and called it decisive; that took the top of a *predicted* range. What decides it is the **spread**: the shortcut is uniform across classes and contamination is not (1.04x-1.37x), and this dataset exists to compare classes and bands. The price is real — `provable` rules **513 of 743** reviewed negatives ineligible — but it loses **none** of the 147 it can hold, and telling those two apart is a change to the coverage gate itself. Two defects found on the way: `apply_corrections` folded verdicts for **any** class, so #3588 adding thirteen classes to the shared `corrections.json` made the shipped twelve-class build die with `KeyError: 'car'`; and `labels_exhaustive` was set by a **one-class review**, which would have admitted images to an "all-provable" pool whose absence claim is still VG's silence for eleven classes (now `coco_scored`, 197 images apart). The pile is **not rebuilt** — deferred so it is built once with #3588 — so `--verify` gains a pool-size and a composition check (#3678, #3681, #3690). |
| [`2026-09-06-cross-class-negatives-3667/`](2026-09-06-cross-class-negatives-3667/REPORT.md) | Rebuild `vg_scale` so a class is scored against images holding a *different* class (#3667), and measure what the rebuild actually bought. Eleven cells over three GRID jobs — `vg_scale`, `vg_scale_any` and `vg_scale_deep` × five embedders — then the built data checked against the price #3672 computed from the shipped pickle, and #3667's own claim tested with two probes. | **The change is real, it is worth +45.2% evaluable images per cell, and the contamination it removes lives almost entirely at one end of the axis this dataset exists to measure.** A head fitted the way the OLD benchmark posed the problem fires on the newly-admitted negatives **1.88 ± 0.19×** as often as on the pool it trained against (5.0% → **9.4%** FPR) — but split by band that is **2.50 ± 0.42** at `@small` against **1.25 ± 0.20** at `@large`, which is not resolvable from 1. So **#3156's published band effect is a lower bound by construction** (#3679). A text probe, which has no shortcut available to it, loses less than half as much AUC (**−0.012 ± 0.005** vs **−0.027 ± 0.007**) — the gap between the two probes *is* the shortcut. The price was good (+46.3% predicted, **+45.2%** measured); it read `categories` where `_evaluable` reads the labels, and **28.9%** of COCO-exhaustive positives hold a class they were never designated for. Three things the rebuild found that nothing else would: the merged fix was a **no-op on `vg_scale_deep`** and was writing 36 junk cell names per image into it (the #3156 guarantee, stated backwards, in a shipped pickle); the shipped cell was **ten days and five merged rulings stale** with nothing in the pile able to say so (#3678); and realised prevalence moved 7.14% → **~5.0%**, so the dataset's own `k*` is half a bit from the constant every study quotes (#3681). Vectors reproduce bit-for-bit on a repeat build and to 3e-07 across two GPU models, but **two of five embedders move by 3e-04 across a membership change** — batch composition, not the machine (#3683). |
| [`2026-09-06-shipped-pool-3666/`](2026-09-06-shipped-pool-3666/REPORT.md) | Has pool error for the twelve classes `vg_scale` actually ships ever been measured (#3666)? #3588 reported a `pool error` column of 0.0-7.1% for thirteen *candidate* classes and none for the shipped twelve, which would leave the benchmark with two tiers of label quality. Read out of the negative pass's 2,400 judgements; nothing rebuilt, `pile_config` untouched. | **It was measured — at 100 uniform draws per class against the thirteen's 70 — and the two tiers are not separable**: shipped **1.40%** [0.68, 2.86] against candidate **2.09%** [1.34, 3.24], a difference of **-0.69 +/- 1.39** pp. The seven classes asked inside a group needed an **attribution**, not another pass: **9 images**, because COCO settles every find on its own half for free, and none of the Vehicles or Table Objects finds is a `bus`, a `bicycle` or a `knife`. The real result is what the finds *are* — **6 of 9 are rulings that do not exist** for these classes (a wristwatch, a clock drawn on a monitor, a departure board, a pop-up canopy, the blank back of a sign), and ruling them out moves the union from **7.0%** to **3.0-5.0%**. So do not buy #3666's 840 draws: 380 more per class buy +/-1.0 pp where one sentence about wristwatches moved `clock` by **3.0** pp (#3673, #3674, #3675, #3676). Also: on the **45%** of the pool COCO scores, COCO reports a shipped class in **0 of 1,888** images and a candidate in **692** — the shipped 0.0% there is by construction, so a uniform draw wastes 45% of a reviewer's attention. |
| [`2026-09-05-pooled-names-3636/`](2026-09-05-pooled-names-3636/REPORT.md) | Can the VG names #3618 could not measure alone be adjudicated as a group (#3636)? 76 of 626 candidates fell below the five-image floor, carrying 312 non-COCO images that were neither acted on nor refuted. `name_evidence.py --pooled` scores a **group** with the same three cuts, counted over images, and lets only a name with no verdict of its own inherit; the grouping itself is declared in `pile_config` — `SCALE_VG_CONSTRUCTIONS` (colour, size, typing, count, spelling, plural) and `SCALE_VG_GROUPS` (hand-declared kinds, each with its criterion). `SCALE_CLASSES` untouched, nothing rebuilt. | **It works, and it is worth a third of what the issue estimated — because most families are not one hypothesis.** The gate that decides is **homogeneity**: over the members with a rate of their own, no member's Wilson interval may exclude the group's member-weighted rate (Bonferroni-adjusted). Constructions fail it **6 times in 69 (9%)**; hand-declared kinds **11 in 19 (58%)**. `bird`/`species` pools 24 names to 33% with **10 of 20 dissenting** — two populations, not noise: `geese` 1.00 and `dove` 1.00 against `chicken` **0.05** over 192 images and `crane` **0.02** over 123, which are the food and machine senses of a bird word. **25 names inherit** a verdict: **+54 repaired**, +18 withheld, **+27 net banded** on the non-COCO half, all of it in `umbrella` (+30), `dog` (+12), `bus` (+7) and `clock` (+5), and **every change an addition** — an individual measurement always wins, so nothing shipped was reversed. The sharpest result is negative: `boat`/`vessel` is sunk by **one member**, the word `vessel` at **0.23** against sixteen others between 0.69 and 1.00, and re-declaring the group after seeing that would be fitting the answer, so it is filed (**#3662**) rather than acted on. `stop sign` gains nothing pooled either — its ten `<what it says> sign` compounds pool to **7%** over 327 images — which is the last thing #3635's verdict could have been checked against. |
| [`2026-09-05-stop-sign-pool/`](2026-09-05-stop-sign-pool/REPORT.md) | Should `stop sign` be repaired, restricted or dropped (#3635)? #3618 found it the worst-served class in *C* — 18.7% self-match, and a 19,148-image `sign` family refuted at 7.9% — and offered a human pass, restriction to the COCO half, or removal from `SCALE_CLASSES`. This measures the thing all three assume. `pile_config` untouched, nothing rebuilt. | **None of the three, and the class is not the one the numbers indict.** Supply is not the problem: after `anchor_to_coco` the class holds **282 / 426 / 298** per band against `SCALE_N_POS` = 100, and the "496 VG images" is VG's *name* count on the half where the labels are COCO's anyway. What is real is **pool contamination**, measured here for the first time (`pool_contamination.py`, the unconditioned question a name-conditioned rate cannot reach): `stop sign` **1.17%**, *fourth* of twelve, against `backpack`'s **2.77%** — 112 expected hidden positives per 3,900-image pool, more than the 100 labelled ones per cell. `sign` still must not be listed, but not for the reason #3618 gave: the "12.7 images withheld" price defends itself on a pool that is **18.4x over-subscribed** (77,119 eligible, 4,200 drawn). A positive control refuted the obvious replacement argument — `bike` concentrates on the pool's hard negatives **more** sharply than `sign` (17.9x vs 6.8x at top-50), so *withholding hard negatives* is what every ambiguous name does. What discriminates is good hard negatives destroyed per contaminant retired, i.e. `1/precision - 1`: `bike` **31 : 30**, `sign` **435 : 37**. Recommendation: keep the class, extend the **shared** negative-pool review that already found 21 of them — one pass repairs all twelve (#3655, #3656, #3657). |
| [`2026-09-05-band-fold-3637/`](2026-09-05-band-fold-3637/REPORT.md) | Is a scattered fold the right outcome (#3637)? `canonicalise` merges an alias spelling's boxes into the class's and `band_for` bands the union, so folding a second spelling pushes 248 already-banded images past the scatter guard and out of every band (`clock` nets **−16**). Three readings — merge and let the guard judge, rescue the class's own band, or only ever add — scored against COCO's exhaustive boxes on the 51,411-image overlap, then run through the real build passes to designation. Nothing rebuilt. | **Keep it scattered, and the class really is scattered.** On the 225 images the modes disagree about, COCO says the class is not a single-band positive on **199 (88%)** — the fold's verdict is right **88%** of the time against **6.7%** for keeping the class's own band (**+81 ± 3.6** pp paired, McNemar χ² = 156), and it wins overall too (76% vs 75%, **+1.8 ± 0.14** pp). The structural argument is bigger than the ledger: `anchor_to_coco` **already** un-bands **17%** of the COCO half on evidence of exactly the same kind, so `guarded` would make the two halves of one dataset disagree about what a positive is. The cost is nil — **no cell** of 36 is under `SCALE_N_POS` under any mode (the scarcest has 144 for 100) and **no mode drops a reviewed image** (130 of the 3,600 designations carry a human verdict, so the hazard #3616 circled had something to hit and hit none of it). Two things fixed on the way: the fold now runs *after* the anchor (a verified no-op — identical ids in all 36 cells) and the old order over-reported the fold **2×**, 5,142 boxes against the 2,559 the build keeps. |
| [`2026-09-04-vg-name-coverage/`](2026-09-04-vg-name-coverage/REPORT.md) | What are the other eleven `vg_scale` classes' VG names worth (#3618)? #3617 fixed the mechanism by which a class built from one spelling turns its own instances into negatives, and declared `bicycle`; this measures all twelve. 626 candidate names, drawn from what lands on COCO's boxes **and** from each class's head-noun family, adjudicated against COCO over the 51,411-image overlap. `SCALE_CLASSES` untouched, nothing rebuilt. | **The measurement the issue specifies cannot make the decision, and the largest column in the study is the one you must not act on.** Fold-in is a box test conditioned the wrong way round: `sign` carries **46.6%** of COCO's `stop sign` boxes and a VG `sign` box is a stop sign **7.9%** of the time — 12.7 images withheld from the shared pool per contaminated negative removed (#3635). The test that decides is **repair precision** — COCO adjudicating the images where a name is the class's only evidence — and `1 / precision` reads directly as that price. 32 names fold, 50 are withheld, 468 refuted: **860 images repaired** and 2,664 withheld on the non-COCO half, `bird` +18%. Three method results: the box-overlap test **cannot see a spelling split** (`backpack`/`backpacks` scores 0.000 both ways, 846 of 1,740 pairs never co-occur); VG's synonym list is a list of **one**, always, so there is no free fix in the reader; and the head-noun shortcut is a trap — the largest member of `dog`'s family is **`hot dog`**, 0 of 181. A fold also un-bands 248 images via the scatter guard, and `clock` nets **−16** (#3637). |
| [`2026-09-03-vg-scale-classes/`](2026-09-03-vg-scale-classes/REPORT.md) | Which classes can `vg_scale` actually carry, and what must their names say (#3588)? A measurement pass over VG supply, measured name aliasing and definition risk for every class the issue proposes, plus the review material for what survives: 13 slates, 13 datasets, 13 empty detectors and an [annotation guide](2026-09-03-vg-scale-classes/ANNOTATION_GUIDE.md). `SCALE_CLASSES` is untouched and `vg_scale` is not rebuilt. | **The issue's own proposal does not survive the gate the issue specifies**, and the reason is structural: every scene-exclusive class it wanted (`train`, `zebra`, `giraffe`, `elephant`) fails on the **small** band — a class that owns its scene is photographed filling the frame — so context exclusivity and small-band supply are anti-correlated in VG, and the easy end of the axis cannot be widened from this source (**#3603**). Thirteen classes survive, none of them from the headline tiers. Definition risk is measurable **before** anyone labels: `coco_folds.py` prints `magazine` (79) and `magazines` (30) under `book` — the split that cost that pass 49 verdicts — and scores `book` at 43%, which calibrates `cell phone` (46%) and `cup`'s 1,136 `glass` boxes. The rule then travels in the **dataset name**, the only string a reviewer sees while voting. The thirteen would also evict 34% of the shared negative pool against 300 spares (**#3604**). |
| [`2026-09-03-drive-cold-3521/`](2026-09-03-drive-cold-3521/REPORT.md) | Does the corrected `--drive` produce a *usable* timing profile (#3521)? Two legs of one workload on one node, differing only in whether the driver arranges the caches a step's cost forks on, each leg the other's held-out set. Scored on **bar error** — the fraction of the progress bar budgeted to the wrong step — reported **per branch**. | **The issue is real, its remedy was aimed at the wrong mechanism, and one of its four instances is misdiagnosed.** A profile fitted as dev's driver produces one puts **0.94** of a dataset-open's bar in the wrong step when the atlas rebuilds — six times worse than shipping **no profile** (0.15) — and the fix trades it for 0.34 there at the cost of 0.49 on the restore, where the old one was perfect. No cell can pace both branches (**#3594**). On the *embed* fork, where the branch can simply be arranged, the win is unambiguous: **0.35 → 0.00**, and the default `--reps 2` was fitting a slope through one real embed and one zero. Two corrections to the tree: `dataset_stage`·`embed` never measured embedding on **any** branch — the demo importer embeds inside `acquire` (**#3593**) — and the "minutes-long" atlas rebuild is **under 9 s** at every size swept, reaching minutes only near the 50k threshold (**#3595**). |
| [`2026-09-02-timing-r2-3345/`](2026-09-02-timing-r2-3345/REPORT.md) | What does the timing profile's r² actually say, now that #3334 keeps one (#3345)? Three profiles read apart: the generic recorder across five task families, the older per-load profiler on the **same** eight imports, and the #3062 resweep's 4,771 rows refit through this fitter. Every fitted step also scored on the prediction error a progress bar would feel. | **`embed` and `finalize` are cleanly affine (r² 1.00 / 0.99, 3 % / 7 % error); every step the shipped pacing thinks *dominant* carries no r² at all, for one reason.** `--drive` measures the **warm** occurrence of each once-per-process cost, so the fitted profile gives the model load **0.00** of a 412-image import where the shipped cost model gives it **0.79**, and 2 % to a coverage atlas the defaults weight at 85 %. A high r² is not a well-paced step (`serialize`: r² 0.99, error 69 %) and a low one is not always a bad step (`coverage`: r² 0.03, error 17 %). Rollup cells — the ones that always match — run r² 0.29 / 50 % error against 1.00 / 3 % exact. Four defects fixed: nothing read the r², both recorders stamped a blank embedder, and `dataset_promote` was silently unmeasurable against a removed endpoint. |
| [`2026-08-31-enrich-descriptions-3127/`](2026-08-31-enrich-descriptions-3127/REPORT.md) | Should `enrich_descriptions` still default to `False`, now that #3077 made `clap_general` the audio default (#3127)? Enrichment on/off for the default embedder of **every** media type - 22 eval datasets, 560 paired (dataset, category) queries - plus `clap` and `bge` controls. Executes [`PLAN.md`](2026-08-31-enrich-descriptions-3127/PLAN.md), pre-registered before the run. | **Keep `False`; the setting is one boolean doing four jobs.** It is worth **+0.014 +/- 0.009** on the audio default (below its own 2 SE, and it is one template - `the sound of {text}` - not the ensemble), inert on `siglip`, and **-0.057 +/- 0.009** on the text default, worse on 45 of 45 categories and reproduced on `bge`. #3077's sign flip reproduces exactly. The E5 query/passage asymmetry, the obvious mechanism, accounts for only a quarter of the damage. Follow-up: make enrichment per-embedder. |
| [`2026-08-30-fit-quality-3329/`](2026-08-30-fit-quality-3329/PREREG.md) | Is the 2-component Gaussian mixture a **good** fit, not just the best one (#3329)? Absolute goodness-of-fit for the mixture that sets every threshold the app computes: tail calibration at the cut, class-conditional shape on the logit axis, component-to-class identification, and whether the shipped k=0.3 anchoring moves the fit at all. 192 cells on `vg_scale_any`, three geometries. | **Pre-registered, not yet run** - the plan is linked because no report exists yet. Every fit diagnostic in the tree is *relative* (one family against another, one cut against another), so a misspecification both families share is invisible to all of them; this is the first absolute measurement. H4 is pre-registered so that "the fit is wrong and it costs nothing" is a reportable finding rather than a null. |
| [`2026-08-29-inclusion-knob-3196/`](2026-08-29-inclusion-knob-3196/REPORT.md) | Does the Inclusion knob still have authority under the shipped linear SVM head (#3196)? 21 stops of the slider x 5 cut rules, two full head arms, 576 cells on `vg_scale`. | **The premise does not reproduce on real data, and the sign is the other way round** - the knob is at least as live under the SVM and significantly livelier in both DINOv3 environments. Nothing ships. The flat band the issue found is real but its axis is the **target**: dead-step rate rises monotonically small -> large under both heads, and 7 of the 8 individually dead region cells are `@large` (`clock@large` in all four seeds). |
| [`2026-08-28-voted-exclusion-3308/`](2026-08-28-voted-exclusion-3308/PLAN.md) | Did the #3308 voted-media exclusion buy anything on real data, and is `EXCLUSION_MIN_REMAINDER = 60` the right floor (#3312)? Two stages: production scale, where the floor is inert, and deep voting on a modest collection, where the remainder crosses every arm's floor. | **Pre-registered, not yet run** — the plan is linked because no report exists yet. Both numbers behind the shipped floor today are synthetic; the live hypothesis is that the change is a rigor improvement with no measurable benefit at production scale, and the design makes that a reportable finding rather than a null. |
| [`2026-08-28-calibration-fold-count-3310/`](2026-08-28-calibration-fold-count-3310/REPORT.md) | Does more cross-calibration ever pay, once its wall-clock price is charged (#3314)? Executes [`PLAN.md`](2026-08-28-calibration-fold-count-3310/PLAN.md), pre-registered before the run. | **Keep `calibrate_count = 2`** — the gate closes on **cost, not benefit**. More folds do help, early and only on the DINOv3 geometries (−0.0057 ± 0.0012 at K=6, 1–25 votes, region voting), but every fold count that clears the margin costs ≥ 2.3× the user's per-step retrain against a 1.5× ceiling; K=3 is affordable and half the margin. 86% of a fold's price is the anchored EM — make that cheaper and K=6 lands inside the ceiling. |
| [`2026-08-27-good-mining-3267/`](2026-08-27-good-mining-3267/REPORT.md) | Does a different Autopilot **opening** mine better Goods (#3267)? Eight openings x 24 environments x 42 seeds, 200 clicks each, every cell seeded from a typed query. | **`top_long` (`g8@top,b4@mid`) — just take more off the top**: +5.8 positives/100 clicks and −0.018 cost against `prod`, and +11 against the length-matched control, so it wins on *where* it clicks. Yield falls monotonically with sampling depth and the gain is concentrated in **scarce** categories (+9.5 vs +2.5). The shippable Inclusion lever gets ~65% of it, but only because `k-10` **saturates to `@top`** — it is a lossy way of asking for the top of the sort. |
| [`2026-08-27-calibration-fraction-3287/`](2026-08-27-calibration-fraction-3287/REPORT.md) | What Train/Calibrate split should a detector use (#3287)? `calibration_fraction` has been `0.5` since it was introduced — the obvious default, never measured — and it sits on the shipped threshold path. | **0.5 is not optimal on the shipped default**, and the axis is the **embedder, not the voting mode**: `siglip` wants 0.4 (−0.013 ± 0.003, winning in every vote band), `siglip2_l` and both CLIP capacities want 0.3, and `dinov3_patch` wants 0.5 in *either* voting mode. So the per-mode default the issue proposed would average across a disagreement. Evidence only; no production change proposed. |
| [`2026-08-25-calibration-fold-combine/`](2026-08-25-calibration-fold-combine/REPORT.md) | Pooling fold scores or averaging per-fold cuts (#3115) — two docstrings in the repo assert contradictory premises about whether fold scores are comparable. | **Both are wrong, in different places.** Averaging beats pooling in both voting modes; the comparability premise is **mode-dependent** (quantile space is worth −0.032 on region and costs +0.036 on binary). The contamination argument is **prevented by the stratified splitter** and its cited evidence conflates a degenerate *cut* with a degenerate *holdout*. Production's anchored rule is untouched. |
| [`2026-08-25-vg-scale/`](2026-08-25-vg-scale/REPORT.md) | Where does VTSearch stand on same-class target size (#3156 / #3276)? 12 classes x {small, medium, large} x 5 embedder columns x 20 seeds, shipped defaults, 3600 cells. | A map, not a decision: cost is ~4x higher on sub-patch targets than on large ones and four fifths of it is the RANKING, not the cut; region voting is lowest in every band without removing the size effect; and the column with the best free text sort (`siglip2_l`) takes a median 29 clicks to beat it. Carries an interactive [`viewer.html`](2026-08-25-vg-scale/viewer.html) over every slice. |
| [`2026-08-24-transfer-2883/`](2026-08-24-transfer-2883/REPORT.md) | Is the #2836 decomposition's dominant `transfer` term a real cost (#2883)? 552 cells, [pre-registered](2026-08-24-transfer-2883/PREREG.md) before submission. | **`transfer` is not a cost — it is a variance measured against an optimistic reference.** All four pre-registered hypotheses land on both arms independently; one sub-prediction inside H4 was wrong and is called out. |
| [`2026-08-21-inclusion-cut-rule/`](2026-08-21-inclusion-cut-rule/REPORT.md) | Which cut rule should answer the Inclusion knob (#2865)? Five candidate rules × thirteen stops of the knob, scored at their own inclusion, on the two region-voting environments plus their binary controls. | Keep the shipped `mid_tilt`: it delivers 95% of the slider and nothing beat it without losing materially somewhere. The `mid` cut it replaced is **inert** — one admitted set for the whole knob in every measured step. |
| [`2026-08-19-linhead-convergence-2808/`](2026-08-19-linhead-convergence-2808/REPORT.md) | Is the linear head's spike reduction limited by early stopping (#2808)? 450 cells over two datasets × two embedders. | **No — and the question was built on a misread number** (#2847's conformal-threshold pair, not the threshold we ship). Convergence buys −0.016 ± 0.005 in worst-step regret, vanishes on the default embedder and costs ~5.3× training: **do not raise `TRAIN_EPOCHS`.** What is worth acting on is +1.2 ± 0.5 positives — positives, not spikes, are the binding constraint. |
| [`2026-08-18-fastproc-3146/`](2026-08-18-fastproc-3146/REPORT.md) | Should the image embedders switch to the fast (torchvision) processor (#3146)? | **They already had.** `transformers` 5 removed the `Fast` suffix, so the flip happened silently inside our own `>=4.49` pin. GPU preprocessing — the issue's other proposed fix — is **not** cleared: a 1013-cell power run leaves `cost` and `fnr` grazing the 0.005 margin for 1.09× per cell. What outlives the issue is a **second unrecorded axis on the pile** — which backend built a cell. Narrative version: [`report.html`](2026-08-18-fastproc-3146/report.html). |
| [`2026-08-18-gpu-node-3160/`](2026-08-18-gpu-node-3160/REPORT.md) | Why do two nodes both answering to `gres/gpu:v100` produce `siglip2_l` vectors 1.5e-04 apart (#3160), 30× what the calibration studies resolve? | **A type is not a device — and the device was never the problem.** Two V100 parts fed the same input compute bit-identically at every one of 27 blocks; the divergence enters on the **host**, where the 384 px resize rounds differently under AVX-512 than under AVX2. The nine outlier nodes are exactly the nine whose CPUs have no AVX-512. |
| [`2026-08-17-embed-precision-3143/`](2026-08-17-embed-precision-3143/REPORT.md) | Can the image encoders run in half precision (#3143)? Ten arms, precision crossed deliberately with the card. | fp16 is a **very small perturbation** — 2.9e-6 cosine on `siglip2_l`, retrieval order intact, no detectable benchmark effect at 1013 paired cells — so adopt it selectively for the heavy encoders, but **do not flip the default**: `siglip` gains nothing and a global flip would strand the fp32 pile. `bf16` is disqualified. §5 is where the cross-node 1.5e-04 was found, chased in [`2026-08-18-gpu-node-3160/`](2026-08-18-gpu-node-3160/REPORT.md). |
| [`2026-08-12-overview-bench/`](2026-08-12-overview-bench/REPORT.md) | What does a user actually get from each shipped configuration (#3129)? 3 representations × 6 haystacks × typed-vs-clicked, at production defaults. | A characterization, not a ship decision: boxes are worth more than the encoder (strip them and DINOv3 is worse than the shipped default), target scale is the strongest axis, positives are the binding constraint, and VG's own labels bound how good any of these numbers can look. |
| [`2026-08-12-calibration-fold-count/`](2026-08-12-calibration-fold-count/REPORT.md) | Is 2 still the right number of cross-calibration folds (#2897)? A counterfactual screen plus a live A/B, decision rules fixed as module constants before the run. | **Keep `calibrate_count = 2`; the study ships nothing.** Binary voting says so outright — no K beats 2 by the margin and more folds monotonically hurt in the deep regime. Region voting mechanically recommends K=6, but at 2.68× the interactive retrain, with its live effect below the ship margin once acquisition feedback moves, and the mechanism check that was meant to validate it **cannot be read** (a defect in the instrument, not a result). Follow-ups #3115, #3116. |
| [`2026-08-07-acquisition-inclusion/`](2026-08-07-acquisition-inclusion/REPORT.md) | Should the acquisition threshold be decoupled from the reporting threshold? (follow-up to #2847 / PR #2873) | Shipped; the design now lives in [`docs/ML.md`](../ML.md#threshold-calibration). |
| [`2026-08-07-spike-check-2847/`](2026-08-07-spike-check-2847/REPORT.md) | Do the MLP-era cost spikes survive today's stack (#2847)? | Better — and it is mostly the *threshold*, not the head. |
| [`2026-08-05-population-anchored-calibration/`](2026-08-05-population-anchored-calibration/REPORT.md) | Fuse the haystack distribution into the trained threshold instead of scheduling it out (#2852 / #2853). Two runs: deep-regime, then anchor-mass sweep. | Fold-anchored mixture at κ=0.3 with the `mid` cut; fusion does **not** beat the blend on binary voting — a live regression tracked in [`population-anchored-calibration.md`](../plans/population-anchored-calibration.md). |
| [`2026-09-02-docmarks-v2/`](2026-09-02-docmarks-v2/REPORT.md) | The full-scale DocMarks build (#3343): all four sources, 200,000 pages, three nested tiers. At 130x v1's size, is the corpus one an eval can be trusted on? Narrative: [`report.html`](2026-09-02-docmarks-v2/report.html). | Yes for SPODS and Tobacco800, no for UCSF. Four defects a green build had hidden: StaVer contributed **zero pages** (mask stems carry a `-px` suffix the scans do not); Tobacco800 contributed **zero classes** (hardcoded out of clustering, because its signatures ship identities and its 432 logos do not); the Tobacco800 contamination guard was **inert** (`industry` is indexed but not stored in UCSF's Solr, so every page recorded `null` and the same-archive exclusion never fired); and the 200k budget was **unreachable** under an even six-way industry split, three of which are nearly empty -- Fossil Fuel has 311 documents in total. One threshold also does not fit four sources: swept separately, StaVer wants 0.04, SPODS 0.10 and Tobacco800 0.18, where it yields 5x the classes. UCSF letterhead bands are **not clustered at any threshold** -- the band is a fixed top-of-page crop, so the descriptor measures page layout, and the largest component is already 12.4% at the grid's lowest value; at 0.10 it made one 12,706-instance "class". |
| [`2026-08-31-docmarks-v1/`](2026-08-31-docmarks-v1/REPORT.md) | Does fixing the mask decomposition fix the DocMarks class inventory (#3361)? Same 1,088 SPODS pages, same generator, marks drawn correctly. Narrative: [`report.html`](2026-08-31-docmarks-v1/report.html). | Yes — median mark 276→376 px, the 46% "largest mark" and all 1,196 `text` pseudo-marks gone, and v0's bimodal size spike exposed as the fragments it always was (84% of marks now in one 256–512 px bucket). It also moved a number nobody expected: the clustering threshold is a property of the marks, so v0's 0.16 now chains 31.8% of the corpus into one class while still reporting 310. Re-swept to 0.10. |
| [`2026-08-31-docmarks-v0/`](2026-08-31-docmarks-v0/REPORT.md) | What does the DocMarks stamp-detection corpus actually look like? A look-at-it pass over 1,088 SPODS pages and 432 UCSF distractors — no scoring. Narrative: [`report.html`](2026-08-31-docmarks-v0/report.html). | The data is right and the labels are not yet. Marks sit entirely above the 32 px floor at 0.76% of a page (bimodal, so band rather than average). Looking caught three silent defects counts could not: inverted masks reading the paper as one page-sized mark, a fixture-tuned threshold putting 87% of marks in one component while reporting 34 classes, and a 64-bit hash unable to separate a book stamp from an elephant stamp. |
| [`2026-08-04-gmm-cut/`](2026-08-04-gmm-cut/REPORT.md) | Where should the GMM path place its cut (#2836)? Autopilot simulation plus a synthetic bench with known ground truth. | See BLUF; the #2846 remeasurement and the #2881 tail-α run ([pre-registration](2026-08-04-gmm-cut/PREREG-2881.md), [result](2026-08-04-gmm-cut/REPORT-2881.md)) close the EVT cut line — beating production now needs a better *fit*, not a better cut. |
| [`2026-08-04-mixin-schedule/`](2026-08-04-mixin-schedule/REPORT.md) | How safe should safe-thresholds be (#2841) — how much GMM, for how long? | Neither voting mode should ever hand over: `slow_cap50` for region, `cap50` for binary. |
| [`2026-08-03-safe-thresholds/`](2026-08-03-safe-thresholds/REPORT.md) | Should `safe_thresholds` be forced on for everyone (#2799)? | Yes — and the setting was later deleted rather than left as a way to opt into a worse threshold. |
| [`2026-07-31-calibration/`](2026-07-31-calibration/REPORT.md) | Trained vs oracle thresholds for Autopilot (#2781) — run twice, before and after PR #2784. | The runaway threshold was the conformal walk's `k=0` anchor, not the `NO_GOOD_THRESHOLD` sentinel; the raw-patch tree is calibration-bottlenecked and no pre-registered remedy recovers it. |
| [`2026-07-29-max-patch/`](2026-07-29-max-patch/REPORT.md) | Does the HAC region tree earn its keep against raw-patch max-pooling? | Ship tree-free MaxPatch; drop the HAC tree from ingest (#2886). |
| [`2026-07-27-inclusion-knob/`](2026-07-27-inclusion-knob/REPORT.md) | Why doesn't the Inclusion slider move the included set (#2693), and what mechanism would? | Plus [`SELECTION-BIAS.md`](2026-07-27-inclusion-knob/SELECTION-BIAS.md), the study of what the detector's own sort costs the calibration set. |
| [`2026-07-22-mlp-vs-svm/`](2026-07-22-mlp-vs-svm/REPORT.md) | Would a linear or RBF SVM rank better than the detector MLP? | Keep the MLP — no variant met the pre-registered switch criterion. **Superseded as a product decision:** it measured the auto-sized MLP head, and the shipped head has since moved to a linear SVM; see [`docs/ML.md`](../ML.md#the-three-heads-which-one-is-shipped-and-why). Narrative version: [`report.html`](2026-07-22-mlp-vs-svm/report.html). |
| [`2026-07-22-vtsbrowse-umap-tuning/`](2026-07-22-vtsbrowse-umap-tuning/REPORT.md) | How should UMAP's dials be set for the Browse projection? ~5,000 scored fits over 23 embed sets. Narrative-only: [`report.html`](2026-07-22-vtsbrowse-umap-tuning/report.html) is the record. | `n_neighbors` tracks the embedder (10 image / 15 audio), `min_dist` barely matters, and compaction consistently costs layout quality — so it **ships off**. |
| [`2026-07-13-screenshot-iconography/`](2026-07-13-screenshot-iconography/REPORT.md) | Does structural search work on flat rasters — screenshots and scans? The OpenLogo follow-up. Narrative-only: [`report.html`](2026-07-13-screenshot-iconography/report.html) is the record. | **SIFT is the bottleneck on line art** (5.1% vs SuperPoint+LightGlue's 41% true-pair verify); SP+LG as ranker beats SigLIP on both document corpora — the first time structural search wins on a real corpus. 3-DoF is a free precision win; LightGlue needs its own inlier floor (~24); 224 px tiling rescues small targets. |
| [`2026-07-12-toponymy-image-signposts/`](2026-07-12-toponymy-image-signposts/REPORT.md) | Can Toponymy name the neighbourhoods of an *image* browse map? 8 image→text strategies across photos, screenshots and scans, including re-fitting signs over a Find result. Narrative-only: [`report.html`](2026-07-12-toponymy-image-signposts/report.html) is the record. | Shipped the winning strategy into `vtscore/projection/signpost_texts.py`. |
| [`2026-07-12-toponymy-audio-signposts/`](2026-07-12-toponymy-audio-signposts/REPORT.md) | The audio half: what is the right audio→text strategy to feed Toponymy? 4,445 clips, 4 strategies, 2 namers. Narrative-only: [`report.html`](2026-07-12-toponymy-audio-signposts/report.html) is the record. | Shipped the winning strategy into `vtscore/projection/signpost_texts.py`. |
| [`2026-07-11-structural-search-openlogo/`](2026-07-11-structural-search-openlogo/REPORT.md) | Seeded with one real-world logo crop over 27k unstaged photos: how far does SIFT+VLAD get, does 6-DoF beat 4-DoF, and what do labels buy? Narrative-only: [`report.html`](2026-07-11-structural-search-openlogo/report.html) is the record. | **Stage-1 VLAD recall is the ceiling** — 2.5% of true instances reach the top-50, against SigLIP cosine's ~10× stronger showing. 4-DoF wins; on the structural path labels are calibration, not learning. Also caught the deterministic **30th-vote transient** in the shared MLP trainer. |
