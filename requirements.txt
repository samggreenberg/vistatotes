# Core application dependencies
flask
numpy
torch
torchvision
transformers
accelerate
scikit-learn
requests
tqdm

# Per-media-type dependencies.
# Each media type declares its own requirements in its subdirectory.
# To add a new media type, create vistatotes/media/<yourtype>/requirements.txt
# and add a -r line below pointing to it.
-r vistatotes/media/audio/requirements.txt
-r vistatotes/media/video/requirements.txt
-r vistatotes/media/image/requirements.txt
-r vistatotes/media/text/requirements.txt
